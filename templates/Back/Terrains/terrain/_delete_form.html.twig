<form method="post" action="{{ path('app_terrain_delete', {'id': terrain.id}) }}" onsubmit="return confirm('Voulez-vous supprimer ce terrain?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ terrain.id) }}">
    <button class="btn">Delete</button>
</form>
