<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Terrain</title>
    <link href="{{ asset('Back/assets/vendor/bootstrap/css/bootstrap.min.css') }}" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('Front/css/userprofile.css') }}">
    <style>
        body {
            background-color: #ededed;
            color: #033e6a;
            font-family: Arial, sans-serif;
        }

        .form-container {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
        }

        /* Autres styles CSS ici */

        /* Styles pour le formulaire */
        .form-group {
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
        }

        input[type="text"],
        input[type="file"],
        select {
            width: 100%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            margin-top: 4px;
            margin-bottom: 10px;
        }

        button.btn {
            background-color: #4CAF50;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button.btn:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>
<div class="bg"></div>
<div class="bg bg2"></div>
<div class="bg bg3"></div>
<div class="quarter-circle">
    <a href="{{ path('First') }}" style="text-decoration:none;font-size:50px;color:#4CAF30">&larr;</a>
</div>
<main id="main" class="main">
    <div class="container">
        <div class="form-container">
            {% block body %}
            <h2 class="card-title">Ajouter un terrain</h2>
            {{ include('Back/Terrains/terrain/_form.html.twig') }}

             {% endblock %}
        </div>
    </div>
</main>
</body>
</html>
