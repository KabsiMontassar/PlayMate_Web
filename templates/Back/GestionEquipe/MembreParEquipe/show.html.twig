<!DOCTYPE html>

<title>Membreparequipe</title>

{% block body %}
    <h1>Membreparequipe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ membreparequipe.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_membreparequipe_index') }}">back to list</a>

    <a href="{{ path('app_membreparequipe_edit', {'id': membreparequipe.id}) }}">edit</a>

    {{ include('Back/Gestionequipe/MembreParEquipe/_delete_form.html.twig') }}
{% endblock %}
