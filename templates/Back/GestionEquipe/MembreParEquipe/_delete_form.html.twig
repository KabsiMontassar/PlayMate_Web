<form method="post" action="{{ path('app_membreparequipe_delete', {'id': membreparequipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ membreparequipe.id) }}">
    <button class="btn">Delete</button>
</form>
